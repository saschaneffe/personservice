# Use postgres/example user/password credentials
version: '3.1'

services:

  postgre-db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: test123
    ports:
    - 5432:5432